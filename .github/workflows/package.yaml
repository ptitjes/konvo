# This file was generated using Kotlin DSL (.github/workflows/package.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Package'
on:
  workflow_dispatch: {}
jobs:
  check_yaml_consistency:
    name: 'Check YAML consistency'
    runs-on: 'ubuntu-latest'
    steps:
    - id: 'step-0'
      name: 'Check out'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Execute script'
      run: 'rm ''.github/workflows/package.yaml'' && ''.github/workflows/package.main.kts'''
    - id: 'step-2'
      name: 'Consistency check'
      run: 'git diff --exit-code ''.github/workflows/package.yaml'''
  package-x64-linux:
    name: 'Package on Linux x64'
    runs-on: 'ubuntu-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      name: 'Checkout'
      uses: 'actions/checkout@v6'
    - id: 'step-1'
      name: 'Install Java'
      uses: 'actions/setup-java@v5'
      with:
        java-version: '21'
        distribution: 'adopt'
    - id: 'step-2'
      name: 'Build'
      uses: 'gradle/actions/setup-gradle@v6'
    - id: 'step-3'
      name: 'Set up cache'
      uses: 'actions/cache@v5'
      with:
        path: '~/.gradle/caches'
        key: '${{ runner.os }}-gradle-${{ hashFiles(''**/*.gradle*'') }}'
        restore-keys: '${{ runner.os }}-gradle-'
    - id: 'step-4'
      name: 'Build'
      run: './gradlew :apps:desktop-app:packageReleaseDeb :apps:desktop-app:packageReleaseRpm'
    - id: 'step-5'
      name: 'Upload artifacts'
      uses: 'actions/upload-artifact@v7'
      with:
        name: 'linux-x64-packages'
        path: |-
          apps/desktop-app/build/compose/binaries/main-release/deb
          apps/desktop-app/build/compose/binaries/main-release/rpm
  package-arm64-linux:
    name: 'Package on Linux arm64'
    runs-on: 'ubuntu-24.04-arm'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      name: 'Checkout'
      uses: 'actions/checkout@v6'
    - id: 'step-1'
      name: 'Install Java'
      uses: 'actions/setup-java@v5'
      with:
        java-version: '21'
        distribution: 'adopt'
    - id: 'step-2'
      name: 'Build'
      uses: 'gradle/actions/setup-gradle@v6'
    - id: 'step-3'
      name: 'Update dependencies'
      run: 'sudo apt-get update'
    - id: 'step-4'
      name: 'Install libEGL'
      run: 'sudo apt install -y libegl1'
    - id: 'step-5'
      name: 'Set up cache'
      uses: 'actions/cache@v5'
      with:
        path: '~/.gradle/caches'
        key: '${{ runner.os }}-gradle-${{ hashFiles(''**/*.gradle*'') }}'
        restore-keys: '${{ runner.os }}-gradle-'
    - id: 'step-6'
      name: 'Build'
      run: './gradlew :apps:desktop-app:packageReleaseDeb :apps:desktop-app:packageReleaseRpm'
    - id: 'step-7'
      name: 'Upload artifacts'
      uses: 'actions/upload-artifact@v7'
      with:
        name: 'linux-arm64-packages'
        path: |-
          apps/desktop-app/build/compose/binaries/main-release/deb
          apps/desktop-app/build/compose/binaries/main-release/rpm
  package-windows:
    name: 'Package on Windows'
    runs-on: 'windows-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      name: 'Checkout'
      uses: 'actions/checkout@v6'
    - id: 'step-1'
      name: 'Install Java'
      uses: 'actions/setup-java@v5'
      with:
        java-version: '21'
        distribution: 'adopt'
    - id: 'step-2'
      name: 'Build'
      uses: 'gradle/actions/setup-gradle@v6'
    - id: 'step-3'
      name: 'Set up cache'
      uses: 'actions/cache@v5'
      with:
        path: '~/.gradle/caches'
        key: '${{ runner.os }}-gradle-${{ hashFiles(''**/*.gradle*'') }}'
        restore-keys: '${{ runner.os }}-gradle-'
    - id: 'step-4'
      name: 'Build'
      run: './gradlew :apps:desktop-app:packageReleaseMsi'
    - id: 'step-5'
      name: 'Upload artifacts'
      uses: 'actions/upload-artifact@v7'
      with:
        name: 'windows-packages'
        path: 'apps/desktop-app/build/compose/binaries/main-release/msi'
  package-macos:
    name: 'Package on MacOS'
    runs-on: 'macos-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      name: 'Checkout'
      uses: 'actions/checkout@v6'
    - id: 'step-1'
      name: 'Install Java'
      uses: 'actions/setup-java@v5'
      with:
        java-version: '21'
        distribution: 'adopt'
    - id: 'step-2'
      name: 'Build'
      uses: 'gradle/actions/setup-gradle@v6'
    - id: 'step-3'
      name: 'Set up cache'
      uses: 'actions/cache@v5'
      with:
        path: '~/.gradle/caches'
        key: '${{ runner.os }}-gradle-${{ hashFiles(''**/*.gradle*'') }}'
        restore-keys: '${{ runner.os }}-gradle-'
    - id: 'step-4'
      name: 'Build'
      run: './gradlew :apps:desktop-app:packageReleaseDmg'
    - id: 'step-5'
      name: 'Upload artifacts'
      uses: 'actions/upload-artifact@v7'
      with:
        name: 'macos-packages'
        path: 'apps/desktop-app/build/compose/binaries/main-release/dmg'
